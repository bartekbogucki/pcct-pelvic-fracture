# ─────────────────────────────────────────────────────────────────────────────
# Pelvic Fracture Segmentation Pipeline — Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Install all:
#   pip install -r requirements.txt
#
# For macOS (Apple Silicon):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
#
# For HPC (CUDA 11.8):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu118

# ── Core scientific stack ─────────────────────────────────────────────────────
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
scikit-learn>=1.3
scikit-image>=0.21

# ── Medical imaging I/O ───────────────────────────────────────────────────────
pydicom>=2.4
SimpleITK>=2.3
nibabel>=5.1

# ── Deep learning ─────────────────────────────────────────────────────────────
# PyTorch — install separately depending on platform:
#   macOS (MPS):  pip install torch torchvision
#   Linux/CUDA:   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   CPU only:     pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.1
torchvision>=0.16

# ── nnU-Net ───────────────────────────────────────────────────────────────────
# Self-configuring 3D medical image segmentation framework
nnunetv2>=2.2

# ── SAM3 ─────────────────────────────────────────────────────────────────────
# Install from source (not on PyPI):
#   git clone https://github.com/bowang-lab/SAM3
#   cd SAM3 && pip install -e .
# sam3  # listed here for reference only

# ── Image processing ──────────────────────────────────────────────────────────
Pillow>=10.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65
